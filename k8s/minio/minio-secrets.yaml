# MinIO Credentials Secret
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: minio
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/part-of: mlops-workshop
type: Opaque
stringData:
  # Root credentials for MinIO
  root-user: "minioadmin"
  root-password: "minioadmin123"
  # Access keys for applications
  access-key: "mlops-access-key"
  secret-key: "mlops-secret-key-12345"

